[project]
name = "webmtr"
version = "0.1.0"
description = "A simple web based traceroute using mtr"
requires-python = "==3.14.*"
dependencies = [
    "dnspython>=2.8.0",
    "flask==3.1.3",
    "gunicorn==25.3.0",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
